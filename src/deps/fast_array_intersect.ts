// I ran some benchmarks - this can be up to HUNDREDS of times faster than the
// standard library's std/collections/intersect.ts.
export { default as intersect } from "https://esm.sh/fast_array_intersect@1.1.0";
